apiVersion: apps/v1
kind: Deployment
metadata:
  name: paxarisglobal-admin-yatrify-backend
  annotations:
    argocd-image-updater.argoproj.io/image-list: "app=devopspaxarisglobalrepo/paxarisglobal-admin-yatrify-backend"
    argocd-image-updater.argoproj.io/app.update-strategy: latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paxarisglobal-admin-yatrify-backend
  template:
    metadata:
      labels:
        app: paxarisglobal-admin-yatrify-backend
    spec:
      initContainers:
        - name: wait-for-postgres
          image: postgres:16-alpine
          command:
            - sh
            - -c
            - until pg_isready -h paxarisglobal-admin-yatrify-backend-postgres -p 5432 -U yatrify_user; do echo waiting for postgres; sleep 2; done
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: paxarisglobal-admin-yatrify-backend
          image: "devopspaxarisglobalrepo/paxarisglobal-admin-yatrify-backend:latest"
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: DB_URL
              value: jdbc:postgresql://paxarisglobal-admin-yatrify-backend-postgres:5432/yatrify
            - name: DB_USERNAME
              value: yatrify_user
            - name: DB_PASSWORD
              value: yatrify_pass
            - name: REDIS_HOST
              value: paxarisglobal-admin-yatrify-backend-redis
            - name: REDIS_PORT
              value: "6379"
---
apiVersion: v1
kind: Service
metadata:
  name: paxarisglobal-admin-yatrify-backend
spec:
  selector:
    app: paxarisglobal-admin-yatrify-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32405
  type: NodePort
