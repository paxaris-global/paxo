apiVersion: apps/v1
kind: Deployment
metadata:
  name: paxaristest-admin-backend-test-frontend
  annotations:
    argocd-image-updater.argoproj.io/image-list: devopspaxarisglobalrepo/paxaristest-admin-backend_test-frontend
    argocd-image-updater.argoproj.io/paxaristest-admin-backend_test-frontend.update-strategy: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: paxaristest-admin-backend-test-frontend
  template:
    metadata:
      labels:
        app: paxaristest-admin-backend-test-frontend
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: paxaristest-admin-backend-test-frontend
          image: devopspaxarisglobalrepo/paxaristest-admin-backend_test-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: paxaristest-admin-backend-test-frontend
spec:
  selector:
    app: paxaristest-admin-backend-test-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
