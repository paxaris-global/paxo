apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yatrify-admin-testyatrify-backend-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: yatrify-admin-testyatrify-backend-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yatrify-admin-testyatrify-backend-postgres
  template:
    metadata:
      labels:
        app: yatrify-admin-testyatrify-backend-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: yatrify
            - name: POSTGRES_USER
              value: yatrify_user
            - name: POSTGRES_PASSWORD
              value: yatrify_pass
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: yatrify-admin-testyatrify-backend-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: yatrify-admin-testyatrify-backend-postgres
spec:
  selector:
    app: yatrify-admin-testyatrify-backend-postgres
  ports:
    - port: 5432
      targetPort: 5432
