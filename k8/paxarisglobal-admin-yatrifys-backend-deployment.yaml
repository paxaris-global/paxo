apiVersion: apps/v1
kind: Deployment
metadata:
  name: paxarisglobal-admin-yatrifys-backend
  annotations:
    argocd-image-updater.argoproj.io/image-list: "app=devopspaxarisglobalrepo/paxarisglobal-admin-yatrifys-backend"
    argocd-image-updater.argoproj.io/app.update-strategy: latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paxarisglobal-admin-yatrifys-backend
  template:
    metadata:
      labels:
        app: paxarisglobal-admin-yatrifys-backend
    spec:
  initContainers:
    - name: wait-for-postgres
      image: postgres:16-alpine
      command:
        - sh
        - -c
        - until pg_isready -h paxarisglobal-admin-yatrifys-backend-postgres -p 5432 -U yatrify_user; do echo waiting for postgres; sleep 2; done
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: paxarisglobal-admin-yatrifys-backend
          image: "devopspaxarisglobalrepo/paxarisglobal-admin-yatrifys-backend:latest"
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: DB_URL
              value: jdbc:postgresql://paxarisglobal-admin-yatrifys-backend-postgres:5432/yatrify
            - name: DB_USERNAME
              value: yatrify_user
            - name: DB_PASSWORD
              value: yatrify_pass
            - name: REDIS_HOST
              value: paxarisglobal-admin-yatrifys-backend-redis
            - name: REDIS_PORT
              value: "6379"
---
apiVersion: v1
kind: Service
metadata:
  name: paxarisglobal-admin-yatrifys-backend
spec:
  selector:
    app: paxarisglobal-admin-yatrifys-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32413
  type: NodePort
